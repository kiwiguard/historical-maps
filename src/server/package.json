{
    "scripts": {
        "start": "node server.js",
        "build": "cd .. && cd .. && npm install && npm run build",
        "test": "react-scripts test",
        "eject": "react-scripts eject"
    }
}